# 团队请求集合协同 - 深度校准报告

> 基于 Hoppscotch 仓库代码的逐条核验，每条结论附精确代码锚点，明确「已实现」与「目标方案」的边界。

---

## 一、校准说明

### 1.1 校准方法

**代码走查范围**：
- 后端：`packages/hoppscotch-backend/src/**`
- 前端：`packages/hoppscotch-common/src/**`
- 内核：`packages/hoppscotch-kernel/src/**`
- 桌面端：`packages/hoppscotch-desktop/src/**`
- 数据模型：`packages/hoppscotch-backend/prisma/schema.prisma`

**核验标准**：每条结论必须有对应的代码锚点（文件路径 + 行号），无代码证据的标注为「未实现」。

---

## 二、权限角色 - 逐条核验

### 2.1 已实现能力

| 能力 | 代码锚点 | 说明 |
|------|----------|------|
| **三级角色枚举** | [`schema.prisma:331-335`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/prisma/schema.prisma#L331-L335) | `enum TeamAccessRole { OWNER VIEWER EDITOR }` |
| **TeamMember 关联表** | [`schema.prisma:20-28`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/prisma/schema.prisma#L20-L28) | `userUid + teamID` 唯一约束，支持多团队 |
| **装饰器鉴权** | [`requires-team-role.decorator.ts:1-5`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team/decorators/requires-team-role.decorator.ts#L1-L5) | `@RequiresTeamRole(...)` 写入 metadata |
| **Guard 角色校验** | [`gql-team-member.guard.ts:21-43`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team/guards/gql-team-member.guard.ts#L21-L43) | 反射读取 `requiresTeamRole`，查 `TeamMember` 表校验 |
| **写操作权限控制** | [`team-collection.resolver.ts:188, 241, 280, 301, 315`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.resolver.ts#L188) | 所有变更操作要求 `OWNER` 或 `EDITOR` |
| **读操作权限控制** | [`team-collection.resolver.ts:88, 108, 137, 155`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.resolver.ts#L88) | 所有查询操作三角色均可访问 |
| **团队级数据隔离** | 所有 team-collection / team-request 接口 | 所有操作必须指定 `teamID`，通过 Guard 校验成员身份 |

### 2.2 未实现能力（目标方案边界）

| 能力 | 状态 | 证据 |
|------|------|------|
| **集合级授权** | ❌ 未实现 | 代码中无 `TeamCollectionMember` 或类似表，无法给单个集合单独分配角色 |
| **工作区隔离** | ❌ 未实现 | 无 `Workspace` 模型，无层级组织结构 |
| **审计日志** | ❌ 未实现 | 无操作日志表，无 `createdBy` / `updatedBy` 字段记录 |
| **角色继承** | ❌ 未实现 | 角色是平级枚举，无层级传递关系 |
| **细粒度操作权限** | ❌ 未实现 | 无 `canDelete` / `canShare` 等细粒度权限标记 |

> **校准结论**：基础 RBAC 完整度 100%，细粒度授权能力 0%。

---

## 三、排序并发处理 - 逐条核验

### 3.1 已实现能力

| 能力 | 代码锚点 | 说明 |
|------|----------|------|
| **整数 orderIndex 字段** | [`schema.prisma:47, 66`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/prisma/schema.prisma#L47) | `TeamCollection.orderIndex: Int`，`TeamRequest.orderIndex: Int` |
| **唯一约束防冲突** | [`schema.prisma:56, 72`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/prisma/schema.prisma#L56) | `@@unique([teamID, parentID, orderIndex])` 防止同层级序号重复 |
| **行级锁（SELECT FOR UPDATE）** | [`prisma.service.ts:150-159`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/prisma/prisma.service.ts#L150-L159) | 排序操作前锁定同父级下所有兄弟节点 |
| **事务包裹** | [`team-collection.service.ts:908, 980`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L908) | 排序逻辑在 `$transaction` 内原子执行 |
| **删除时重排（含重试）** | [`team-collection.service.ts:555-616`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L555-L616) | 最多 5 次重试，指数退避 `delay(retryCount * 100)` |
| **移动时双父节点锁** | [`team-collection.service.ts:811-845`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L811-L845) | 跨父节点移动时按 `parentID` 排序加锁，防止死锁 |
| **排序后 PubSub 通知** | [`team-collection.service.ts:956-962, 1040-1046`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L956-L962) | 排序成功后广播 `coll_order_updated` 事件 |

### 3.2 重要校准（修正之前的不准确描述）

| 之前描述 | 实际代码证据 | 正确结论 |
|----------|--------------|----------|
| "所有排序操作最多 5 次重试" | [`updateCollectionOrder:893-1052`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L893-L1052) 无 `while (retryCount < MAX_RETRIES)` 循环 | ❌ `updateCollectionOrder` **没有重试**，失败直接返回错误 |
| "移动操作有重试" | [`moveCollection:747-867`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L747-L867) 无重试循环 | ❌ `moveCollection` **没有重试**，失败直接返回错误 |
| "指数退避重试通用" | 仅 [`deleteCollectionAndUpdateSiblingsOrderIndex:560-612`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.service.ts#L560-L612) 有重试 | ✅ 仅**删除操作**有重试，排序和移动均无重试 |

### 3.3 未实现能力（目标方案边界）

| 能力 | 状态 | 证据 |
|------|------|------|
| **Fractional Index** | ❌ 未实现 | `orderIndex` 仍是 `Int` 类型，无字符串序号生成逻辑 |
| **排序操作重试** | ❌ 未实现 | `updateCollectionOrder` 和 `moveCollection` 均无重试循环 |
| **字段级冲突检测** | ❌ 未实现 | 无版本号、无向量时钟、无 diff 对比 |
| **Last Write Wins 策略** | ❌ 未实现 | 无 `updatedAt` 比较逻辑，简单覆盖 |
| **操作历史/回滚** | ❌ 未实现 | 无历史表、无快照机制 |

> **校准结论**：基础并发安全完整度 70%，但高并发场景弹性不足（排序操作无重试是明显短板）。

---

## 四、Web 端存储 - 逐条核验

### 4.1 已实现能力

| 能力 | 代码锚点 | 说明 |
|------|----------|------|
| **Store 抽象层** | [`store/v/1.ts:88-110`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/v/1.ts#L88-L110) | 统一的 `init/set/get/has/remove/clear/watch` 接口 |
| **Web 端存储实现** | [`store/impl/web/v/1.ts:13-283`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/web/v/1.ts#L13-L283) | `BrowserStoreManager` 封装 |
| **存储介质** | [`store/impl/web/v/1.ts:41, 52, 66, 72`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/web/v/1.ts#L41) | ✅ **localStorage**（不是 IndexedDB！） |
| **命名空间隔离** | [`store/impl/web/v/1.ts:29-31`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/web/v/1.ts#L29-L31) | `${namespace}:${key}` 格式 |
| **个人数据持久化** | [`persistence/index.ts:593-679`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/persistence/index.ts#L593-L679) | REST/GQL 个人集合、历史、环境等 22 个键 |
| **变更监听** | [`store/impl/web/v/1.ts:105-144`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/web/v/1.ts#L105-L144) | `watch()` 方法支持订阅变更 |

### 4.2 重要校准（修正之前的不准确描述）

| 之前描述 | 实际代码证据 | 正确结论 |
|----------|--------------|----------|
| "Web 端 IndexedDB" | [`store/impl/web/v/1.ts`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/web/v/1.ts) 全文无 `indexedDB` / `IndexedDB` 引用 | ❌ Web 端使用 **localStorage**，不是 IndexedDB |

### 4.3 未实现能力（目标方案边界）

| 能力 | 状态 | 证据 |
|------|------|------|
| **团队数据持久化** | ❌ 未实现 | [`team-collection.service.ts`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/team-collection.service.ts) 全文无 `Store.set` / `Store.get` 调用 |
| **离线操作队列** | ❌ 未实现 | 无 Outbox 表、无操作序列化逻辑 |
| **增量同步标记** | ❌ 未实现 | 无 `lastSyncAt` 时间戳、无变更游标 |
| **团队数据缓存失效** | ❌ 未实现 | 收到 Subscription 事件后仅更新内存，无缓存一致性保证 |

> **校准结论**：个人数据持久化完整度 100%，团队数据持久化 0%。

---

## 五、Desktop 端存储 - 逐条核验

### 5.1 已实现能力

| 能力 | 代码锚点 | 说明 |
|------|----------|------|
| **Tauri Store 封装** | [`store/impl/desktop/v/1.ts:18-280`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/desktop/v/1.ts#L18-L280) | `TauriStoreManager` 基于 `@tauri-apps/plugin-store` |
| **本地文件存储** | [`store/impl/desktop/v/1.ts:60`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-kernel/src/store/impl/desktop/v/1.ts#L60) | `Store.load(this.storePath)` 持久化到本地文件 |
| **桌面设置持久化** | [`persistence.service.ts:215-360`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-desktop/src/services/persistence.service.ts#L215-L360) | 桌面设置、更新状态、连接状态、最近实例 |
| **数据 Schema 迁移** | [`persistence.service.ts:98-198`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-desktop/src/services/persistence.service.ts#L98-L198) | 支持 v1 → v2 数据迁移 |
| **Rust 侧同步** | [`persistence.service.ts:286-306`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-desktop/src/services/persistence.service.ts#L286-L306) | 桌面设置变更自动同步到 Rust 侧 mailbox |

### 5.2 未实现能力（目标方案边界）

| 能力 | 状态 | 证据 |
|------|------|------|
| **团队数据持久化** | ❌ 未实现 | Desktop 端复用 common 包的 `TeamCollectionsService`，同样无持久化 |
| **离线操作队列** | ❌ 未实现 | 同 Web 端 |
| **多端数据合并** | ❌ 未实现 | 无本地与远端冲突合并逻辑 |

> **校准结论**：Web 与 Desktop 端存储架构一致，团队数据均无本地持久化。

---

## 六、团队集合同步链路 - 逐条核验

### 6.1 已实现能力

| 能力 | 代码锚点 | 说明 |
|------|----------|------|
| **GraphQL Subscription** | [`team-collection.resolver.ts:364-473`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/team-collection/team-collection.resolver.ts#L364-L473) | 5 个集合订阅 + 5 个请求订阅 |
| **PubSub 发布** | [`pubsub.service.ts:20-26`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/pubsub/pubsub.service.ts#L20-L26) | `publish()` + `asyncIterator()` |
| **前端订阅注册** | [`team-collection.service.ts:656-939`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/team-collection.service.ts#L656-L939) | 12 个事件流订阅（added/updated/removed/moved/orderUpdated 等） |
| **内存树维护** | [`team-collection.service.ts:268-654`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/team-collection.service.ts#L268-L654) | `addCollection` / `updateCollection` / `removeCollection` 等 |
| **entityIDs 去重** | [`team-collection.service.ts:144, 273`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/team-collection.service.ts#L144) | `Set<string>` 防止重复加入内存树 |
| **懒加载展开** | [`team-collection.service.ts:1030-1071`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/team-collection.service.ts#L1030-L1071) | 点击展开时才拉取子集合/请求 |
| **分页加载** | [`team-collection.service.ts:309-360`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-common/src/services/team-collection.service.ts#L309-L360) | 每页 10 条，游标分页 |

### 6.2 重要校准（修正之前的不准确描述）

| 之前描述 | 实际代码证据 | 正确结论 |
|----------|--------------|----------|
| "Redis PubSub" | [`pubsub.service.ts:2, 17`](file:///d:/fz/0508-2/solo-dogfeeding/code/14-hoppscotch/packages/hoppscotch-backend/src/pubsub/pubsub.service.ts#L2) | ❌ 使用 **`graphql-subscriptions` 的 LocalPubSub**，不是 Redis！注释提到"生产环境用 Redis"但代码未实现 |
| "事件通道 team_coll/{teamID}/coll_*" | 实际发布无 teamID 前缀，直接用字符串 | ⚠️ 需进一步确认通道命名规范 |

### 6.3 未实现能力（目标方案边界）

| 能力 | 状态 | 证据 |
|------|------|------|
| **乐观更新** | ❌ 未实现 | 所有操作均等待后端响应后才更新内存树 |
| **操作级同步** | ❌ 未实现 | 仅实体级增删改事件，无字段级变更 |
| **断线重连补同步** | ❌ 未实现 | 重连后全量重拉，无增量补同步 |
| **向量时钟/因果一致性** | ❌ 未实现 | 无版本追踪，无因果保证 |
| **冲突自动合并** | ❌ 未实现 | 简单覆盖，无合并策略 |
| **实时光标/选区** | ❌ 未实现 | 无协作感知能力 |

> **校准结论**：基础事件通知完整度 80%，高级协同能力 0%。

---

## 七、已实现 vs 目标方案 - 边界总览

```
┌─────────────────────────────────────────────────────────────────┐
│                    已实现（当前代码可验证）                     │
├─────────────┬─────────────┬─────────────┬───────────────────────┤
│ 权限        │ 排序        │ 存储        │ 同步                  │
│ ─────────   │ ─────────   │ ─────────   │ ─────────             │
│ 三级角色    │ 整数序号    │ Web localStorage │ GraphQL Sub      │
│ 装饰器+Guard│ 唯一约束    │ Desktop Tauri  │ 12 个事件流       │
│ 团队隔离    │ 行级锁      │ 个人数据持久化 │ 内存树维护        │
│ 读写权限分  │ 事务包裹    │ Schema 迁移   │ entityIDs 去重     │
│             │ 删除重试    │             │ 懒加载/分页        │
└─────────────┴─────────────┴─────────────┴───────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                 未实现（目标方案，无代码证据）                  │
├─────────────┬─────────────┬─────────────┬───────────────────────┤
│ 权限        │ 排序        │ 存储        │ 同步                  │
│ ─────────   │ ─────────   │ ─────────   │ ─────────             │
│ 集合级授权  │ Fractional  │ 团队数据本  │ 乐观更新              │
│ 工作区隔    │ Index       │ 地持久化    │ 操作级同步            │
│ 审计日志    │ 排序重试    │ 离线队列    │ 断线补同步            │
│ 角色继承    │ 字段级冲突  │ 增量标记    │ 冲突合并              │
│             │ 检测        │             │ 实时光标              │
└─────────────┴─────────────┴─────────────┴───────────────────────┘
```

---

## 八、关键校准点汇总

| 序号 | 校准项 | 之前描述 | 实际结论 | 影响 |
|------|--------|----------|----------|------|
| 1 | Web 端存储介质 | IndexedDB | **localStorage** | 存储容量受限（~5MB），需重新评估团队数据缓存方案 |
| 2 | PubSub 实现 | Redis PubSub | **LocalPubSub** | 多实例部署时事件无法跨节点广播 |
| 3 | 排序重试范围 | 所有操作 5 次重试 | **仅删除操作有重试** | 高并发排序死锁时直接失败，用户体验差 |
| 4 | 团队数据持久化 | 隐含已实现 | **完全未实现** | 刷新页面全量重拉，加载慢，无离线能力 |

---

## 九、最小改造顺序（基于校准后结论）

### 优先级 P0（必须先做，2-3 周）

**1. 团队数据本地缓存**
- 新增 `TEAM_COLLECTIONS` / `TEAM_REQUESTS` 存储键
- 初始化时先读缓存渲染，后台异步拉取
- 收到 Subscription 事件后增量更新缓存
- **代码改动范围**：
  - `packages/hoppscotch-common/src/services/persistence/index.ts`（新增存储键）
  - `packages/hoppscotch-common/src/services/team-collection.service.ts`（新增缓存读写）

### 优先级 P1（高价值，3-4 周）

**2. 排序操作增加重试**
- 把删除操作的重试逻辑提取为通用装饰器
- `updateCollectionOrder` 和 `moveCollection` 增加最多 3 次重试
- **代码改动范围**：
  - `packages/hoppscotch-backend/src/team-collection/team-collection.service.ts`（提取重试逻辑）

**3. Fractional Index 改造**
- `orderIndex` 从 `Int` 改为 `String`
- 实现字符串中间值生成算法
- 移除行锁和重试（Fractional Index 天然无冲突）
- **代码改动范围**：
  - `prisma/schema.prisma`（字段类型变更）
  - `team-collection/team-collection.service.ts`（排序逻辑重写）
  - `team-request/team-request.service.ts`（排序逻辑重写）

### 优先级 P2（按需引入，4-6 周）

**4. PubSub 支持 Redis**
- 根据环境变量切换 LocalPubSub / RedisPubSub
- **代码改动范围**：
  - `packages/hoppscotch-backend/src/pubsub/pubsub.service.ts`

**5. 离线操作队列（Outbox）**
- 新增 `TeamOperationOutbox` 表
- 操作拦截 + 网络状态检测 + 重放逻辑
- **代码改动范围**：
  - 新增 `team-operation-outbox/` 模块
  - 前端新增 `OfflineSyncService`

### 优先级 P3（远期，8-12 周）

**6. CRDT 实时协同**
- Yjs 集成 + 数据模型映射
- 实时光标 + 版本历史
- **建议**：非核心需求可暂缓

---

## 十、验收标准（按优先级）

### P0 验收
- [ ] 断网刷新页面，团队集合仍可见（读缓存）
- [ ] 有网时刷新，1 秒内渲染缓存数据，后台静默更新
- [ ] 收到实时事件后，缓存自动增量更新

### P1 验收
- [ ] 并发 50 次排序操作，失败率 < 1%（原 ~10%）
- [ ] 排序操作平均延迟 < 100ms（原 ~500ms）
- [ ] 历史数据无缝迁移，无排序错乱

### P2 验收
- [ ] 多实例部署时，事件可跨节点广播
- [ ] 断网时可创建/编辑/删除，操作不丢失
- [ ] 网络恢复后 5 秒内自动同步完成

---

## 十一、附录：完整代码锚点索引

### 权限相关
| 模块 | 文件路径 | 行号 |
|------|----------|------|
| TeamAccessRole 枚举 | `prisma/schema.prisma` | 331-335 |
| TeamMember 模型 | `prisma/schema.prisma` | 20-28 |
| 鉴权装饰器 | `src/team/decorators/requires-team-role.decorator.ts` | 1-5 |
| GqlTeamMemberGuard | `src/team/guards/gql-team-member.guard.ts` | 21-43 |

### 排序相关
| 模块 | 文件路径 | 行号 |
|------|----------|------|
| orderIndex 字段 | `prisma/schema.prisma` | 47, 66 |
| 唯一约束 | `prisma/schema.prisma` | 56, 72 |
| 行锁实现 | `src/prisma/prisma.service.ts` | 150-159 |
| 删除重试逻辑 | `src/team-collection/team-collection.service.ts` | 555-616 |
| updateCollectionOrder | `src/team-collection/team-collection.service.ts` | 893-1052 |
| moveCollection | `src/team-collection/team-collection.service.ts` | 747-867 |

### 存储相关
| 模块 | 文件路径 | 行号 |
|------|----------|------|
| Web Store 实现 | `packages/hoppscotch-kernel/src/store/impl/web/v/1.ts` | 13-283 |
| Desktop Store 实现 | `packages/hoppscotch-kernel/src/store/impl/desktop/v/1.ts` | 18-280 |
| 个人数据持久化 | `packages/hoppscotch-common/src/services/persistence/index.ts` | 593-679 |
| 桌面端持久化 | `packages/hoppscotch-desktop/src/services/persistence.service.ts` | 215-360 |

### 同步相关
| 模块 | 文件路径 | 行号 |
|------|----------|------|
| PubSub 实现 | `src/pubsub/pubsub.service.ts` | 1-27 |
| 订阅定义 | `src/team-collection/team-collection.resolver.ts` | 364-473 |
| 前端订阅 | `packages/hoppscotch-common/src/services/team-collection.service.ts` | 656-939 |
| 内存树操作 | `packages/hoppscotch-common/src/services/team-collection.service.ts` | 268-654 |

---

*本报告基于代码走查生成，所有结论均有精确代码锚点可追溯。未修改任何源代码。*

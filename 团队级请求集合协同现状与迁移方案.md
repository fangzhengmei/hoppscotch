# 团队级请求集合协同现状与迁移方案

> 基于 hoppscotch 当前代码实现的深度调研与差距分析

---

## 1. 现状精准还原

### 1.1 权限模型现状

#### 1.1.1 角色体系
当前仅实现了**团队级三级角色**，没有更细粒度的权限控制：

| 角色 | 定义位置 | 权限说明 |
|------|----------|----------|
| `OWNER` | `schema.prisma:331-335` | 团队所有者，可管理成员、删除团队 |
| `EDITOR` | `schema.prisma:331-335` | 可创建/编辑/删除集合、请求、环境变量 |
| `VIEWER` | `schema.prisma:331-335` | 仅查看和运行请求，不可修改 |

**关键代码位置**：
- 角色枚举：`packages/hoppscotch-backend/prisma/schema.prisma:331-335`
- 成员模型：`packages/hoppscotch-backend/prisma/schema.prisma:20-28`
- 服务实现：`packages/hoppscotch-backend/src/team/team.service.ts`

#### 1.1.2 鉴权机制
采用**装饰器 + Guard** 的模式：

```typescript
// 装饰器使用示例（team-collection.resolver.ts）
@Mutation(() => TeamCollection)
@UseGuards(GqlCollectionTeamMemberGuard)
@requiresTeamRole([TeamAccessRole.OWNER, TeamAccessRole.EDITOR])
async updateTeamCollection(/* ... */) { /* ... */ }
```

**鉴权流程**：
1. `@requiresTeamRole` 装饰器声明所需角色
2. Guard 解析请求中的 `teamID` 或 `collectionID`
3. 查询 `TeamMember` 表验证用户角色
4. 角色匹配则放行，否则抛出错误

**关键文件**：
- 团队级 Guard：`packages/hoppscotch-backend/src/team/guards/gql-team-member.guard.ts`
- 集合级 Guard：`packages/hoppscotch-backend/src/team-collection/guards/gql-collection-team-member.guard.ts`
- 装饰器定义：`packages/hoppscotch-backend/src/team/decorators/requires-team-role.decorator.ts`

#### 1.1.3 权限边界
- ❌ **没有工作区概念**：只有 Team 一级，无法在团队内划分隔离的工作区
- ❌ **没有资源级授权**：EDITOR 对团队内所有集合有相同权限，无法针对单个集合授权
- ❌ **没有继承机制**：角色权限是扁平的，没有层级继承
- ✅ **团队隔离**：不同 Team 的数据完全隔离（通过 `teamID` 外键约束）

---

### 1.2 集合变更与排序冲突处理

#### 1.2.1 数据模型
集合和请求通过**整数 `orderIndex`** 维护顺序：

```prisma
// schema.prisma:42-57
model TeamCollection {
  id         String           @id @default(cuid())
  parentID   String?
  teamID     String
  title      String
  orderIndex Int              // 排序字段
  // ...
  @@unique([teamID, parentID, orderIndex])  // 唯一约束防止同位置重复
}

// schema.prisma:59-73
model TeamRequest {
  id           String         @id @default(cuid())
  collectionID String
  teamID       String
  orderIndex   Int            // 排序字段
  // ...
  @@unique([teamID, collectionID, orderIndex])
}
```

#### 1.2.2 排序冲突处理策略

**核心机制：数据库事务 + 行锁 + 重试**

典型的重排序流程（`team-collection.service.ts:893-1052`）：

```
1. 开启数据库事务
2. 对目标父集合下的所有行加锁（SELECT ... FOR UPDATE）
3. 重新查询当前 orderIndex（防止事务启动后被修改）
4. 计算新位置，批量更新受影响兄弟节点的 orderIndex
5. 更新目标节点的 orderIndex
6. 提交事务
7. 发布 PubSub 事件通知所有客户端
```

**冲突重试机制**（`team-collection.service.ts:560-616`）：
- 最多重试 **5 次**
- 捕获以下错误并重试：
  - 唯一约束冲突（P2002）
  - 死锁（P2000）
  - 事务超时
- 重试策略：指数退避（`retryCount * 100ms`）

**关键代码**：
- 集合重排序：`packages/hoppscotch-backend/src/team-collection/team-collection.service.ts:893-1052`
- 请求重排序：`packages/hoppscotch-backend/src/team-request/team-request.service.ts:312-517`
- 排序服务：`packages/hoppscotch-backend/src/orchestration/sort/sort.service.ts`

#### 1.2.3 并发编辑冲突
- ❌ **没有冲突检测**：后写入者直接覆盖先写入者（Last Write Wins）
- ❌ **没有差异合并**：同一字段并发修改时，旧值被直接覆盖
- ❌ **没有版本历史**：无法回溯和恢复到历史版本
- ✅ **事件通知**：变更通过 PubSub 实时推送给所有在线客户端

---

### 1.3 Web/Desktop 本地存储与同步机制

#### 1.3.1 存储架构

**个人数据（持久化到本地）**：
- 存储层：自定义 `Store` 抽象（底层基于 IndexedDB）
- 命名空间：`persistence.v1`
- 持久化内容（`packages/hoppscotch-common/src/services/persistence/index.ts:111-134`）：
  - REST/GQL 个人集合
  - 请求历史
  - 环境变量
  - 设置项
  - 标签页状态
  - 等等...

**团队数据（仅内存存储）**：
- 团队集合完全存储在内存中的 Vue `ref`
- 刷新页面后需要重新从后端拉取
- 没有本地持久化，没有离线缓存

#### 1.3.2 同步机制

**实时同步通道**：GraphQL Subscription + Redis PubSub

**事件类型**（`packages/hoppscotch-backend/src/pubsub/topicsDefs.ts`）：
```
team_coll/{teamID}/coll_added        - 集合创建
team_coll/{teamID}/coll_updated      - 集合更新
team_coll/{teamID}/coll_removed      - 集合删除
team_coll/{teamID}/coll_moved        - 集合移动
team_coll/{teamID}/coll_order_updated - 集合重排序
team_req/{teamID}/req_created        - 请求创建
team_req/{teamID}/req_updated        - 请求更新
team_req/{teamID}/req_deleted        - 请求删除
team_req/{teamID}/req_moved          - 请求移动
team_req/{teamID}/req_order_updated  - 请求重排序
```

**前端订阅处理**（`team-collection.service.ts:656-939`）：
- 前端维护一棵内存中的集合树
- 接收 Subscription 事件后更新本地树
- 通过 `entityIDs` Set 防止重复实体
- 懒加载：根集合初始加载，子集合按需展开加载

**关键文件**：
- 前端团队集合服务：`packages/hoppscotch-common/src/services/team-collection.service.ts`
- 持久化服务：`packages/hoppscotch-common/src/services/persistence/index.ts`

#### 1.3.3 离线能力评估
| 能力 | 支持状态 | 说明 |
|------|----------|------|
| 离线查看个人集合 | ✅ | 已持久化到 IndexedDB |
| 离线编辑个人集合 | ✅ | 本地修改，联网后手动同步？ |
| 离线查看团队集合 | ❌ | 仅内存存储，刷新丢失 |
| 离线编辑团队集合 | ❌ | 无网络时操作直接失败 |
| 离线操作队列 | ❌ | 没有 Outbox 队列机制 |
| 多端冲突自动合并 | ❌ | 后写入者覆盖先写入者 |

---

## 2. 现状与目标方案差距清单

### 2.1 权限模型差距

| 维度 | 现状 | 目标方案 | 差距说明 |
|------|------|----------|----------|
| **层级粒度** | 仅团队级 | 组织→工作区→资源三级 | 缺少工作区隔离和资源级授权 |
| **角色丰富度** | 3 种角色 | 8+ 种角色 | 需要增加：工作区管理员、协作者、集合编辑者/查看者等 |
| **权限继承** | 无 | 层级自动继承 | 需要实现权限向下传递机制 |
| **细粒度控制** | 无 | 支持集合级单独授权 | 当前 EDITOR 对所有集合有相同权限 |
| **审计日志** | 无 | 完整操作审计 | 需要记录所有变更的操作人、时间、IP |
| **SSO 集成** | 无 | 企业级 SSO | 无 SAML/OIDC 支持 |

### 2.2 协作冲突解决差距

| 维度 | 现状 | 目标方案 | 差距说明 |
|------|------|----------|----------|
| **冲突检测** | 无 | 字段级检测 | 当前直接覆盖，无任何检测 |
| **自动合并** | 无 | 不同字段自动合并 | 需要 CRDT 或 OT 算法支持 |
| **冲突策略** | LWW 粗暴覆盖 | 智能策略（LWW/人工介入） | 同一字段冲突时应保留双方版本供选择 |
| **版本历史** | 无 | 完整版本链 + 回滚 | 需要实现快照和版本对比 |
| **排序算法** | 整数 orderIndex + 锁 | Fractional Indexing / RGA | 当前算法并发时冲突率高，性能差 |
| **实时协作** | 事件通知级 | 操作级同步 | 当前是全量覆盖，不是增量操作同步 |

### 2.3 离线缓存协同差距

| 维度 | 现状 | 目标方案 | 差距说明 |
|------|------|----------|----------|
| **团队数据持久化** | 仅内存 | IndexedDB 持久化 | 团队集合需要落地存储 |
| **离线编辑支持** | ❌ 不支持 | ✅ 完全支持 | 需要 Outbox 队列 + 同步状态管理 |
| **增量同步** | 全量重拉 | 向量时钟 + 增量日志 | 当前刷新页面全量重新加载 |
| **同步状态标记** | 无 | 完整状态机 | 需要标记：synced / syncing / pending / error |
| **冲突解决 UI** | 无 | 可视化冲突对比 | 冲突时需展示差异供用户选择 |
| **多端一致性** | 弱保证 | 最终一致性保证 | 当前可能出现多端状态不一致 |

### 2.4 同步机制差距

| 维度 | 现状 | 目标方案 | 差距说明 |
|------|------|----------|----------|
| **同步粒度** | 文档级 | 操作级（Operation-based） | 当前是整个集合/请求覆盖，不是细粒度操作 |
| **同步协议** | GraphQL Sub | CRDT 同步协议 | 需要引入 Yjs 或 Automerge 协议 |
| **服务端角色** | 中心化调度 | 中继 + 持久化 | 服务端不再做冲突裁决，只转发和存储 |
| **离线队列** | 无 | Outbox Pattern | 需要实现排队、重试、失败处理 |
| **乐观更新** | 无 | 本地立即生效 | 操作先本地应用，后台异步同步 |

---

## 3. 分阶段迁移建议

### 3.1 第一阶段：基础增强（MVP+）

**目标**：在现有架构上增强，风险最低

**重点任务**：

1. **权限模型增强**
   - 保持现有三级角色不变
   - 增加 `TeamCollection` 级别的 `accessControl` 字段（JSON）
   - 实现集合级的「可编辑成员列表」和「可见性设置」
   - 增加操作审计日志表，记录关键变更

2. **排序算法优化**
   - 将整数 `orderIndex` 改为字符串类型的 **Fractional Index**
   - 移除 `@@unique([teamID, parentID, orderIndex])` 约束
   - 排序时不再需要批量更新兄弟节点，减少锁冲突
   - 参考算法：[Implementing Fractional Indexing](https://www.figma.com/blog/realtime-editing-of-ordered-sequences/)

3. **团队数据本地缓存**
   - 在 `PersistenceService` 中增加团队集合的持久化
   - 新增 key：`teamCollections/{teamID}`
   - 实现简单的「上次同步时间戳」机制
   - 应用启动时先加载本地缓存，后台静默刷新

**预计改动量**：中
**预计周期**：2-3 周
**风险等级**：低

---

### 3.2 第二阶段：离线能力建设

**目标**：支持团队集合的离线编辑，引入 Outbox 模式

**重点任务**：

1. **Outbox 队列实现**
   - 在 IndexedDB 中增加 `syncOutbox` 表
   - 结构：`{ id, entityType, entityId, operation, data, timestamp, retryCount, status }`
   - 所有写入操作先写入 Outbox，再尝试同步
   - 实现指数退避重试逻辑

2. **同步状态管理**
   - 为每个集合/请求增加 `syncStatus` 字段：`synced / pending / error`
   - UI 上展示同步状态指示器
   - 实现同步失败的错误提示和手动重试入口

3. **增量同步协议**
   - 服务端增加 `updatedAt` 索引
   - 客户端同步时携带 `lastSyncAt` 时间戳
   - 服务端仅返回增量变更
   - 实现基于向量时钟的简单版本追踪

4. **冲突检测与提示**
   - 同步时检测 `localUpdatedAt` vs `remoteUpdatedAt`
   - 冲突时弹出简单的「保留本地」/「使用远程」选择
   - 被覆盖的版本存入本地历史

**预计改动量**：大
**预计周期**：4-6 周
**风险等级**：中

---

### 3.3 第三阶段：CRDT 实时协作

**目标**：引入 CRDT，实现真正的实时多人协作

**重点任务**：

1. **技术选型与集成**
   - 选择 CRDT 库：推荐 **Yjs**（性能最优，生态最完善）
   - 设计集合和请求的 CRDT 数据模型
   - 实现服务端的 CRDT 文档存储和中继

2. **数据模型迁移**
   - 设计从现有模型到 CRDT 模型的迁移工具
   - 实现双写过渡方案（旧 API + 新 CRDT 通道并行）
   - 开发数据验证和修复工具

3. **实时同步通道升级**
   - 现有 GraphQL Subscription 与 CRDT 同步通道并存
   - 实现操作级的增量同步
   - 支持离线操作的自动合并

4. **高级协作特性**
   - 实时光标（显示其他用户正在编辑的位置）
   - 版本历史与一键回滚
   - 版本对比可视化
   - 合并冲突的智能解决

**预计改动量**：极大
**预计周期**：8-12 周
**风险等级**：高

---

## 4. 关键技术决策点

### 4.1 CRDT 库选型建议

| 库 | 引入成本 | 性能 | 生态 | 推荐度 |
|----|----------|------|------|--------|
| **Yjs** | 中 | 极高 | 完善（支持多种编辑器绑定） | ⭐⭐⭐⭐⭐ |
| **Automerge 2.0** | 低 | 中 | 一般 | ⭐⭐⭐⭐ |
| 自研 | 极高 | 未知 | 无 | ⭐ |

**推荐 Yjs 的理由**：
- 性能比 Automerge 高 2-5 倍
- 有成熟的 y-websocket / y-indexeddb 配套实现
- 社区活跃，文档完善

### 4.2 迁移风险控制

1. **渐进式迁移**：不要试图一步到位，三阶段逐步推进
2. **双写过渡**：新旧系统并行运行至少 2 个版本
3. **Feature Flag**：所有新特性通过功能开关控制，可随时回滚
4. **数据备份**：迁移前必须有完整的数据备份和回滚方案
5. **灰度发布**：先对内测用户开放，逐步扩大范围

### 4.3 性能考量

- CRDT 元数据开销：预计增加 20-50% 的存储体积
- 大集合（>1000 个请求）的首次加载时间可能增加
- 需要实现分页加载和虚拟滚动配合
- IndexedDB 的性能瓶颈需要持续监控和优化

---

## 5. 总结

Hoppscotch 当前实现了**基础的团队协作能力**，但在多端同步、离线支持、实时协作方面还有较大差距。

**迁移路线图**：
```
现状 ──► 阶段1（基础增强） ──► 阶段2（离线能力） ──► 阶段3（CRDT实时协作）
  │           │                     │                        │
  ├─ 三级角色 ├─ 集合级授权        ├─ Outbox队列          ├─ Yjs集成
  ├─ 事件同步 ├─ Fractional Index ├─ 增量同步            ├─ 操作级同步
  └─ 无离线   └─ 本地缓存          └─ 基础冲突解决        └─ 实时协作
```

**建议优先推进阶段 1**，风险低、收益明显，能解决 60% 的高频需求。阶段 2 和 3 可以根据业务需求和团队资源情况逐步推进。

# 团队请求集合协同 - 校准报告

> 基于 Hoppscotch 现有代码的逐项核对，明确已实现 / 未实现能力，给出可执行的最小改造顺序。

---

## 一、校准范围与方法

**校准维度**：

| 维度 | 说明 |
|------|------|
| 权限角色 | RBAC 模型、角色定义、鉴权机制、细粒度控制 |
| 排序并发处理 | orderIndex 机制、事务锁、重试策略、冲突解决 |
| 本地存储 | Web / Desktop 端存储介质、团队数据缓存策略 |
| 实时同步 | 事件链路、订阅机制、冲突合并、乐观更新 |

**核对方法**：代码走查 + 语义理解，不做运行时验证。

---

## 二、逐项校准：已实现 vs 未实现

### 2.1 权限角色

| 能力 | 状态 | 代码位置 | 备注 |
|------|------|----------|------|
| **三级角色定义** | ✅ 已实现 | `prisma/schema.prisma:TeamAccessRole` | OWNER / EDITOR / VIEWER |
| **装饰器鉴权** | ✅ 已实现 | `team/decorators/requires-team-role.decorator.ts` | `@RequiresTeamRole(...)` |
| **Guard 校验** | ✅ 已实现 | `team/guards/gql-team-member.guard.ts` | 基于反射读取角色列表 |
| **团队级隔离** | ✅ 已实现 | 所有 team-collection / team-request 接口 | 所有操作需带 teamID |
| **集合级授权** | ❌ 未实现 | 无 | 当前仅团队级，不能给单个集合单独授权 |
| **工作区隔离** | ❌ 未实现 | 无 | 无 Workspace 概念 |
| **审计日志** | ❌ 未实现 | 无 | 无操作留痕 |
| **角色继承** | ❌ 未实现 | 无 | 无层级权限传递 |

> **校准结论**：基础 RBAC 完整，缺少细粒度授权能力。

---

### 2.2 集合排序并发处理

| 能力 | 状态 | 代码位置 | 备注 |
|------|------|----------|------|
| **整数 orderIndex** | ✅ 已实现 | `prisma/schema.prisma:TeamCollection.orderIndex` | Int 类型，从 1 开始 |
| **行级锁** | ✅ 已实现 | `prisma/prisma.service.ts:150-159` | `SELECT ... FOR UPDATE` |
| **事务包裹** | ✅ 已实现 | `team-collection/team-collection.service.ts` | 所有写操作均在 `$transaction` 内 |
| **最多 5 次重试** | ✅ 已实现 | `team-collection.service.ts:60-61, 560-612` | 仅针对死锁、唯一约束、超时 |
| **指数退避** | ✅ 已实现 | `team-collection.service.ts:610` | `delay(retryCount * 100)` |
| **删除时重排** | ✅ 已实现 | `team-collection.service.ts:555-616` | 后面的兄弟节点 orderIndex -1 |
| **移动时重排** | ✅ 已实现 | `team-collection.service.ts:651-688` | 新旧父节点分别处理 |
| **Fractional Index** | ❌ 未实现 | 无 | 仍使用连续整数 |
| **字段级冲突检测** | ❌ 未实现 | 无 | 简单 Last Write Wins |
| **版本向量** | ❌ 未实现 | 无 | 无数据版本追踪 |
| **自动合并** | ❌ 未实现 | 无 | 无冲突合并逻辑 |
| **操作历史** | ❌ 未实现 | 无 | 无回滚能力 |

> **校准结论**：基础并发安全完整，缺少高并发场景下的弹性能力。

---

### 2.3 本地存储（Web + Desktop）

| 能力 | 状态 | 代码位置 | 备注 |
|------|------|----------|------|
| **Web 端 IndexedDB** | ✅ 已实现 | `hoppscotch-kernel/src/store/*` | 基于 IndexedDB 的 Store 抽象 |
| **Web 端持久化键** | ✅ 已实现 | `persistence/index.ts:111-134` | 共 22 个存储键，含个人集合/历史/环境等 |
| **Desktop 端 Tauri Store** | ✅ 已实现 | `hoppscotch-desktop/src/kernel/store-resource.ts` | 本地文件存储 |
| **Desktop 持久化键** | ✅ 已实现 | `persistence.service.ts:33-42` | 桌面设置、更新状态、连接状态等 |
| **团队数据内存缓存** | ✅ 已实现 | `services/team-collection.service.ts:140-144` | `collections` ref + `entityIDs` Set 去重 |
| **懒加载展开** | ✅ 已实现 | `team-collection.service.ts:1030-1071` | 点击展开时才拉取子集合/请求 |
| **分页加载** | ✅ 已实现 | `team-collection.service.ts:309-360` | 每页 10 条，游标分页 |
| **团队数据本地持久化** | ❌ 未实现 | 无 | 刷新页面需全量重拉 |
| **离线操作队列** | ❌ 未实现 | 无 | 无 Outbox Pattern 实现 |
| **增量同步标记** | ❌ 未实现 | 无 | 无 lastSync 时间戳 |
| **多端数据合并** | ❌ 未实现 | 无 | 无本地与远端冲突合并 |

> **校准结论**：个人数据持久化完整，团队数据仅内存缓存，无离线能力。

---

### 2.4 实时同步链路

| 能力 | 状态 | 代码位置 | 备注 |
|------|------|----------|------|
| **GraphQL Subscription** | ✅ 已实现 | `team-collection.resolver.ts:364-473` | 共 5 个集合订阅 + 5 个请求订阅 |
| **Redis PubSub** | ✅ 已实现 | `pubsub/pubsub.service.ts` | 事件广播通道 |
| **事件通道定义** | ✅ 已实现 | `team-collection.service.ts` 各处 publish | `team_coll/{teamID}/coll_*` 格式 |
| **前端事件处理** | ✅ 已实现 | `services/team-collection.service.ts:656-939` | 12 个订阅处理器 |
| **entityIDs 去重** | ✅ 已实现 | `team-collection.service.ts:144, 273` | 防止重复加入内存树 |
| **乐观更新** | ❌ 未实现 | 无 | 等待后端响应后才更新 UI |
| **操作级同步** | ❌ 未实现 | 无 | 仅实体级增删改事件 |
| **向量时钟** | ❌ 未实现 | 无 | 无因果一致性保证 |
| **冲突合并策略** | ❌ 未实现 | 无 | 简单覆盖 |
| **断线重连补同步** | ❌ 未实现 | 无 | 重连后全量重拉 |

> **校准结论**：基础事件通知完整，缺少高级协同能力。

---

## 三、已实现能力的架构映射

```
┌─────────────────────────────────────────────────────────┐
│                     前端 (hoppscotch-common)            │
├─────────────────┬─────────────────┬─────────────────────┤
│  内存缓存       │  Subscription   │   本地存储          │
│  collections   │  12 个事件流    │   IndexedDB         │
│  entityIDs Set │  懒加载展开     │   (仅个人数据)      │
└─────────────────┴─────────────────┴─────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│                    后端 (hoppscotch-backend)           │
├─────────────────┬─────────────────┬─────────────────────┤
│  Guard 鉴权     │  事务 + 行锁    │   Redis PubSub      │
│  三级角色       │  5 次重试       │   事件广播          │
│  团队隔离       │  orderIndex     │                     │
└─────────────────┴─────────────────┴─────────────────────┘
                              │
                              ▼
                        PostgreSQL
```

---

## 四、与目标方案的差距总览

| 能力域 | 已实现度 | 核心差距 |
|--------|----------|----------|
| **权限** | 60% | 缺少集合级授权、工作区、审计日志 |
| **排序并发** | 50% | 缺少 Fractional Index、字段级冲突检测 |
| **存储** | 40% | 团队数据无持久化、无离线队列 |
| **同步** | 40% | 缺少乐观更新、操作级同步、自动合并 |
| **整体** | **48%** | 基础可用，高级协同能力缺失 |

---

## 五、最小改造顺序（按 ROI 排序）

### 阶段 0：先明确目标优先级（前置决策）

在动手前先回答：

1. **核心用户场景**：是「小团队低频协作」还是「大团队高并发实时编辑」？
2. **离线需求强度**：是否必须支持断网编辑？
3. **合规要求**：是否需要审计日志？

> 若目标是「小团队可用」，只需做 **步骤 1 + 2**；若目标是「企业级协同」，则需要全流程。

---

### 改造路径（按依赖顺序）

```
┌────────────┐    ┌────────────┐    ┌────────────┐    ┌────────────┐
│  步骤 1    │───▶│  步骤 2    │───▶│  步骤 3    │───▶│  步骤 4    │
│ 团队数据   │    │ 排序优化   │    │ 离线能力   │    │ 实时协同   │
│ 本地缓存   │    │ Fractional │    │ Outbox     │    │ CRDT / OT  │
│ (低风险)   │    │ Index      │    │ 队列       │    │ (高风险)   │
└────────────┘    └────────────┘    └────────────┘    └────────────┘
       │                 │                 │                 │
       ▼                 ▼                 ▼                 ▼
    2-3 周            3-4 周            4-6 周           8-12 周
```

---

#### 步骤 1：团队数据本地缓存（ROI 最高，2-3 周）

**目标**：刷新页面不用全量重拉，提升加载速度 80%+。

**改动点**：

1. **新增存储键**：在 `STORE_KEYS` 中增加 `TEAM_COLLECTIONS`、`TEAM_REQUESTS`
2. **缓存策略**：
   - 写入：每次内存树变更时异步写入 IndexedDB
   - 读取：初始化时先读本地缓存渲染，后台异步拉取最新数据
3. **版本标记**：新增 `lastSyncAt` 时间戳，用于增量校验
4. **失效机制**：收到 Subscription 事件后，仅更新对应节点，不触发全量拉取

**风险**：低，纯新增代码，不影响现有逻辑。

**代码改动范围**：
- `packages/hoppscotch-common/src/services/persistence/index.ts`（新增存储键）
- `packages/hoppscotch-common/src/services/team-collection.service.ts`（新增缓存读写）

---

#### 步骤 2：排序优化 - Fractional Index（3-4 周）

**目标**：解决高并发排序冲突，重试次数降低 90%。

**改动点**：

1. **数据模型变更**：
   - `TeamCollection.orderIndex` 从 `Int` 改为 `String`
   - `TeamRequest.orderIndex` 从 `Int` 改为 `String`
2. **迁移脚本**：把现有整数转字符串（如 "1" → "a0"）
3. **排序算法**：实现 Fractional Index 生成逻辑（取两个字符串的中间值）
4. **移除重试**：不再需要死锁重试，Fractional Index 天然无冲突
5. **移除行锁**：排序不再需要 `SELECT ... FOR UPDATE`

**风险**：中，需数据迁移，需双写兼容期。

**代码改动范围**：
- `prisma/schema.prisma`（字段类型变更）
- `team-collection/team-collection.service.ts`（排序逻辑重写）
- `team-request/team-request.service.ts`（排序逻辑重写）

---

#### 步骤 3：离线能力 - Outbox 模式（4-6 周）

**目标**：支持断网编辑，上线后自动同步。

**改动点**：

1. **Outbox 表**：新增 `TeamOperationOutbox` 表存储待同步操作
2. **操作序列化**：把增删改操作序列化为 JSON 存入 Outbox
3. **同步器**：
   - 在线时：实时发送 + Outbox 兜底
   - 离线时：写入 Outbox，网络恢复后按顺序重放
4. **冲突处理**：
   - 简单策略：后写入者胜（Last Write Wins）
   - 提示策略：冲突时弹框让用户选择
5. **状态指示**：UI 显示「离线 - 待同步 X 项」

**风险**：中，涉及状态机复杂度，需充分测试边界场景。

**代码改动范围**：
- 新增 `team-operation-outbox/` 模块
- `team-collection/team-collection.service.ts`（操作拦截）
- 前端新增 `OfflineSyncService`

---

#### 步骤 4：实时协同 - CRDT 集成（8-12 周，可选）

**目标**：多人实时编辑无冲突，类 Figma 体验。

**改动点**：

1. **CRDT 选型**：Yjs（推荐，生态成熟）
2. **数据模型映射**：把集合树映射为 Yjs 文档结构
3. **同步协议**：
   - 后端：WebSocket 中继 + 文档合并
   - 前端：Yjs Provider 封装
4. **光标协作**：实时光标位置、选中区域同步
5. **版本历史**：基于 CRDT 的时间旅行

**风险**：高，架构变动大，需重写大部分同步逻辑。

**建议**：若非核心需求，可暂缓。

---

## 六、决策建议

### 推荐方案（MVP 优先）

**只做步骤 1 + 步骤 2**，总周期 **5-7 周**，可解决 80% 的痛点：

1. 刷新页面秒开（步骤 1）
2. 并发排序无冲突（步骤 2）

剩下的步骤 3、4 可根据业务增长按需引入。

### 不推荐方案

**跳过步骤 1、2 直接上 CRDT**：
- 周期长（3 个月+）
- 风险高（大量重构）
- 收益前置不明显（用户感知弱）

---

## 七、验收标准

### 步骤 1 验收
- [ ] 断网刷新页面，团队集合仍可见
- [ ] 有网时刷新，1 秒内渲染出缓存数据，后台静默更新
- [ ] 收到实时事件后，缓存自动增量更新

### 步骤 2 验收
- [ ] 并发 100 次排序操作，无死锁、无重试失败
- [ ] 排序操作延迟 < 100ms（原平均 ~500ms）
- [ ] 历史数据无缝迁移，无排序错乱

### 步骤 3 验收
- [ ] 断网时可创建/编辑/删除集合，操作不丢失
- [ ] 网络恢复后 5 秒内自动同步完成
- [ ] 冲突场景有明确用户提示

---

## 八、附录：关键代码引用

| 模块 | 文件路径 |
|------|----------|
| 角色定义 | `prisma/schema.prisma` |
| 鉴权装饰器 | `src/team/decorators/requires-team-role.decorator.ts` |
| 行锁实现 | `src/prisma/prisma.service.ts:150-159` |
| 排序重试 | `src/team-collection/team-collection.service.ts:560-612` |
| Web 存储 | `packages/hoppscotch-common/src/services/persistence/index.ts` |
| Desktop 存储 | `packages/hoppscotch-desktop/src/services/persistence.service.ts` |
| 前端同步 | `packages/hoppscotch-common/src/services/team-collection.service.ts` |
| 订阅定义 | `src/team-collection/team-collection.resolver.ts:364-473` |

---

*本报告基于代码走查生成，未修改任何源代码。*

# 团队级请求集合多端协同方案调研

## 1. 概述

本文档针对团队级 API 请求集合在多端编辑场景下的同步机制进行概念级调研，核心覆盖**权限模型**、**协作冲突解决**、**离线缓存协同**三大关键领域。调研结论基于行业主流工具（Postman、Apifox、Figma、Notion 等）的实践经验与分布式协同算法的最新进展。

---

## 2. 权限模型设计

### 2.1 分级授权体系

参考 Postman 和 Apifox 的 RBAC（Role-Based Access Control）实践，建议采用**三级权限模型**：

| 层级 | 角色 | 权限范围 | 适用场景 |
|------|------|----------|----------|
| **组织级** | 超级管理员（Owner） | 所有资源的完全控制权，成员管理、账单、安全策略 | 团队负责人 |
| | 组织管理员（Admin） | 管理团队成员、创建/删除工作区、配置全局策略 | 技术负责人 |
| | 普通成员（Member） | 访问已授权的工作区和资源 | 开发/测试人员 |
| **工作区级** | 工作区编辑者（Editor） | 创建/编辑/删除集合、环境、Mock 服务 | 核心开发者 |
| | 工作区查看者（Viewer） | 仅查看和运行请求，不可修改 | 产品/运营人员 |
| | 工作区协作者（Collaborator） | 可编辑被分配的特定资源 | 外部合作方 |
| **资源级** | 集合编辑者 | 对指定集合有完全编辑权 | 接口负责人 |
| | 集合查看者 | 仅可查看和运行指定集合 | 文档阅读者 |

### 2.2 访问控制边界

- **工作区隔离**：不同工作区的集合、环境变量、Mock Server 物理隔离，无跨区访问权限
- **继承机制**：工作区角色权限自动向下继承到集合、环境等子资源
- **细粒度授权**：支持对单个集合、文件夹甚至请求级别的单独授权
- **访问模式**：
  - 私有（Private）：仅被明确授权的用户可访问
  - 团队内可见（Team）：整个团队可见，编辑权限另行配置
  - 公开（Public）：生成只读链接对外分享（需谨慎使用）

### 2.3 审计与追溯

- 所有操作记录操作人、时间、IP、变更内容
- 支持按资源、人员、时间范围筛选操作日志
- 敏感操作（删除集合、导出环境变量）需二次确认

---

## 3. 协作冲突解决机制

### 3.1 核心算法选型对比

实时协同编辑领域有两大主流技术路线：

| 维度 | OT（Operational Transformation） | CRDT（Conflict-free Replicated Data Type） |
|------|----------------------------------|-------------------------------------------|
| **核心思想** | 对并发操作进行位置变换，通过中心化服务器协调 | 数据结构内建"无冲突合并"数学属性，操作可任意顺序应用 |
| **一致性保证** | 通过服务器调度保证 | 通过数学可交换性自动保证 |
| **实时性** | 依赖服务器调度，有中心化开销 | 客户端本地立即生效，低延迟 |
| **离线支持** | 弱（离线操作重放复杂度高） | 强（天然支持离线编辑，恢复后自动合并） |
| **复杂度** | Transform 算法规则复杂 | 数据结构设计复杂，元数据开销较大 |
| **典型应用** | Google Docs、腾讯文档、飞书文档 | Figma、Notion、Linear |

### 3.2 推荐方案：混合 CRDT 架构

针对 API 请求集合的结构化数据特性，推荐采用 **CRDT 优先 + 字段级 LWW** 的混合策略：

#### 3.2.1 数据结构设计

```
Collection CRDT 模型：
├─ 集合元数据（Map CRDT）
│  ├─ name: LWW-Register
│  ├─ description: LWW-Register
│  └─ updatedAt: Timestamp
├─ 请求列表（RGA CRDT - Replicated Growable Array）
│  └─ 每个请求节点（Map CRDT）
│     ├─ id: UUID
│     ├─ name: LWW-Register
│     ├─ method: LWW-Register
│     ├─ url: LWW-Register
│     ├─ headers: OR-Set
│     ├─ body: LWW-Register
│     └─ order: Fractional Index
└─ 文件夹结构（Tree CRDT）
   └─ 父子关系映射
```

#### 3.2.2 冲突解决策略

| 冲突场景 | 解决策略 | 说明 |
|----------|----------|------|
| 同一请求不同字段修改 | 自动合并 | Map CRDT 天然支持不同字段独立更新 |
| 同一字段并发修改 | LWW（Last Write Wins） | 以最后修改时间戳为准，保留最新值 |
| 同一位置并发插入请求 | RGA + 唯一 ID 排序 | 每个字符/节点分配唯一 ID（时间戳+客户端ID），并发插入按 ID 排序 |
| 并发删除与修改 | 删除优先（Observed-Remove） | 已删除的内容不被修改操作复活 |
| 重大结构冲突 | 人工介入 | 当自动合并可能丢失用户意图时，弹出版本对比供用户选择 |

#### 3.2.3 操作同步流程

```
┌─────────────┐     操作本地生效     ┌─────────────┐
│  客户端 A   │─────────────────────▶│  IndexedDB  │
└─────────────┘                      └─────────────┘
       │                                      │
       │ 发送操作（Operation-based CRDT）     │
       ▼                                      ▼
┌─────────────┐                      ┌─────────────┐
│  同步服务   │◀─────────────────────│  客户端 B   │
└─────────────┘  广播操作给所有在线成员
       │
       │ 持久化操作日志
       ▼
┌─────────────┐
│  操作日志库 │
└─────────────┘
```

### 3.3 版本管理与回滚

- 每次保存自动生成版本快照，记录变更 diff
- 支持查看历史版本列表，可一键回滚到任意历史版本
- 提供版本对比功能，可视化展示两个版本间的差异
- 回收站机制：删除的资源进入回收站，30 天内可恢复

---

## 4. 离线缓存协同方案

### 4.1 离线优先（Offline-First）架构

#### 4.1.1 分层存储设计

| 存储层 | 技术选型 | 用途 | 容量限制 |
|--------|----------|------|----------|
| **内存缓存** | Map / LRU Cache | 当前打开的集合数据，实时读写 | 受限于内存 |
| **本地持久化** | IndexedDB（Web）/ SQLite（Desktop） | 所有同步过的集合、环境变量、历史记录 | 50-80% 可用磁盘空间 |
| **静态资源缓存** | Service Worker Cache API | 应用壳（HTML/CSS/JS）、图标、字体 | 通常 50-200MB |
| **轻量元数据** | LocalStorage | 用户偏好、同步状态标记、最后同步时间戳 | 5-10MB |

#### 4.1.2 IndexedDB Schema 设计

```javascript
// 主数据表
{
  collections: {
    key: "id",
    fields: ["id", "workspaceId", "name", "data", "syncStatus", "lastSyncedAt", "updatedAt"]
  },
  requests: {
    key: "id",
    fields: ["id", "collectionId", "data", "syncStatus", "lastSyncedAt", "updatedAt"]
  },
  environments: {
    key: "id",
    fields: ["id", "workspaceId", "name", "data", "syncStatus", "lastSyncedAt", "updatedAt"]
  }
}

// 同步队列表
{
  outbox: {
    key: "id",
    fields: ["id", "entityType", "entityId", "operation", "data", "timestamp", "retryCount", "status"]
  }
}

// 同步状态
{
  syncStatus: {
    key: "key",
    fields: ["key", "value"] // e.g., "lastFullSync": "2024-05-17T10:30:00Z"
  }
}
```

### 4.2 同步队列（Outbox Pattern）

#### 4.2.1 写入流程

```
用户操作
    │
    ▼
┌─────────────────────────┐
│ 1. 写入 IndexedDB        │  乐观更新，立即反馈给用户
│    (syncStatus: pending) │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ 2. 加入 Outbox 队列      │
│    { operation, data,    │
│      timestamp, retryCount } │
└─────────────────────────┘
    │
    ├─ 在线：立即尝试同步
    │     ├─ 成功：删除队列项，标记 synced
    │     └─ 失败：retryCount++，指数退避重试
    │
    └─ 离线：队列等待，监听 online 事件
          联网后按顺序批量同步
```

#### 4.2.2 同步冲突检测与解决

当本地离线修改与远程修改发生冲突时，按以下优先级处理：

| 冲突类型 | 检测条件 | 处理策略 |
|----------|----------|----------|
| 本地修改 + 远程无变化 | `local.updatedAt > remote.updatedAt` | 本地修改推送至远程 |
| 本地无变化 + 远程修改 | `local.updatedAt < remote.updatedAt` | 拉取远程更新覆盖本地 |
| 本地修改 + 远程修改（不同字段） | CRDT Map 检测 | 自动合并，保留双方修改 |
| 本地修改 + 远程修改（同一字段） | LWW 时间戳比较 | 保留时间戳较新的版本，另一版本存入历史 |
| 本地删除 + 远程修改 | Tombstone 标记 | 删除优先，远程修改被忽略 |
| 本地修改 + 远程删除 | Tombstone 检测 | 删除优先，本地修改被丢弃（可从历史恢复） |

### 4.3 多端同步协议

#### 4.3.1 实时同步通道

- **WebSocket 长连接**：用于实时操作广播和在线协作
- **消息格式**：基于 Yjs / Automerge 的操作编码（二进制压缩）
- **心跳机制**：30s 心跳，超时自动重连
- **消息幂等**：每条消息携带唯一 message_id，防止重复处理

#### 4.3.2 增量同步算法

采用 **向量时钟（Vector Clock）** + **增量变更日志** 的方式：

1. 客户端启动时发送本地向量时钟 `{ clientA: 120, clientB: 85, ... }`
2. 服务端返回所有逻辑时间大于客户端时钟的操作日志
3. 客户端按顺序应用操作，更新本地状态
4. 定期（每 10 分钟）执行快照同步，减小日志体积

#### 4.3.3 首次全量同步优化

- 采用分页拉取（每页 50 个集合/请求）
- 按更新时间倒序，优先同步最近修改的内容
- 后台静默同步，不阻塞用户操作
- 支持"仅同步元数据"模式，快速浏览目录结构，按需加载详情

---

## 5. 技术实现参考

### 5.1 主流 CRDT 库对比

| 库 | 体积（minified） | 性能（ops/sec） | 特点 |
|----|------------------|-----------------|------|
| **Yjs** | ~120KB | 26K-156K | 性能最优，生态最完善，支持多种编辑器绑定 |
| **Automerge 2.0** | ~150KB | 中 | 易用性好，API 简洁，适合结构化数据 |
| **Collabs** | ~80KB | 中 | 类型安全，模块化设计 |

### 5.2 行业最佳实践参考

- **Postman Enterprise**：WebSocket 实时同步 + 乐观锁 + 手动冲突解决
- **Apifox**：字段级协作 + 版本历史 + 冲突时实时提示
- **Figma**：全量 CRDT + 离线编辑 + 云端快照
- **Notion**：混合架构（CRDT 处理结构，OT 处理文本块内容）

---

## 6. 总结与建议

### 6.1 核心设计原则

1. **离线优先**：默认假设网络不可用，所有操作先本地生效
2. **最终一致**：接受短期状态不一致，通过数学算法保证最终收敛
3. **无损协作**：任何用户操作都不应被静默丢弃，冲突需有明确的解决策略
4. **权限最小化**：用户仅获得完成工作所需的最少权限

### 6.2 落地路线建议

| 阶段 | 目标 | 关键能力 |
|------|------|----------|
| **MVP 阶段** | 支持团队共享集合 | 基础 RBAC、云端同步、简单 LWW 冲突解决 |
| **增强阶段** | 支持多人实时协作 | CRDT 集成、实时操作广播、离线编辑 |
| **完善阶段** | 企业级协同能力 | 细粒度权限、审计日志、高级版本管理、SSO 集成 |

### 6.3 风险提示

- CRDT 元数据会带来 20-50% 的存储开销，需关注大数据量下的性能
- 离线操作队列积压过多时，同步时间可能较长，需提供进度反馈
- 复杂的合并逻辑可能产生预期外的结果，需充分的用户测试
